```c
int main() {
  struct Person person;
  strcpy(person.nickname, "liyangxia");
  strcpy(person.title, "Sweeping Monk");
  strcpy(person.team, "SkyCyberIO");
  strcpy(person.skill, "Clean, sleep in");
  strcpy(person.message, "Stay focused.");
}
```
